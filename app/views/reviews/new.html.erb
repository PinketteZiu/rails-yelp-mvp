<h1>Reviews#new</h1>
<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating %>
  <%= f.submit "Save", class: "btn btn-primary" %>
<% end %>

<div>
  <%= link_to "Back", restaurant_path(@review.restaurant_id) %>
</div>
